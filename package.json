{
  "name": "web-ai-assistant",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "check": "node --check src/index.js && node --check src/api/chat.js && node --check src/api/provider.js && node --check src/frontend/page.js && node --check src/lib/providers/index.js && node --check src/lib/providers/openai-compatible.js && node --check src/lib/providers/workers-ai.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "vitest": "~3.2.0",
    "wrangler": "^4.92.0"
  }
}
